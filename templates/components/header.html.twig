<link href="{{ asset('css/header.css') }}" rel="stylesheet"/>

<div id="header">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 ">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
            <img class="golden_epil_logo" src="{{ asset('images/logo.png') }}" alt="Symfony!"/>
        </a>

        <ul id="main_menu" class="nav nav-pills">
            <li class="nav-item"><a href="/" class="nav-link ">Головна</a></li>
            <li class="nav-item"><a href="{{ path('appointment') }}" class="nav-link ">Записатися на процедуру</a></li>
            <li class="nav-item"><a href="{{ path('courses') }}" class="nav-link ">Курси</a></li>
            <li class="nav-item"><a href="{{ path('about_us') }}" class="nav-link ">Про нас</a></li>
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        Логаут
                    </a></li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">
                        Логін
                    </a></li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('register_page') }}">
                        Реэстрація
                    </a></li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MASTER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('master_cabinet') }}">
                        Кабінет мастера
                    </a></li>
            {% elseif is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_cabinet') }}">
                       Кабінет користувача
                    </a></li>
            {% endif %}

        </ul>
    </header>
</div>
